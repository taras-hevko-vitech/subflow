// Drizzle schema. Core domain tables (users, bank_connections, accounts, transactions,
// merchants, subscriptions, subscription_events, detection_feedback) land in subF-7.
// Kept empty on purpose so the DB connection and drizzle-kit tooling are wired without
// pre-empting that ticket.
export {};
