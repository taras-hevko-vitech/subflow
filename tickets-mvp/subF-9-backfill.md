# subF-9 · Бекфіл виписки (12 місяців)
**Фаза:** 1 · **Розмір:** M · **Блокери:** subF-8

## Задачі
- [ ] pg-boss pipeline: на підключення рахунку → план вікон ≤31 доба за 12 міс, свіжі першими (останні 2 міс — пріоритет 1)
- [ ] Виконавець вікна: statement(from,to) → якщо повернулось 500 транзакцій, вікно не вичерпане → зсунути `to` = time останньої отриманої − 1s → повторити всередині вікна
- [ ] Пауза між викликами відповідно до лімітера subF-8 (черга сама «дихає» раз на 60с на токен)
- [ ] Upsert транзакцій (ідемпотентно); resume: стан плану у backfill_progress, рестарт продовжує з місця падіння
- [ ] Після кожного завершеного вікна → подія `window.completed` → тригер детекції (subF-11) → часткові результати доступні API
- [ ] `GET /connections/:id/backfill` — прогрес для UI («3 з 12 місяців»)
- [ ] Після повного бекфілу → подія `backfill.completed` → пуш «аналіз завершено» (subF-16)

## Acceptance Criteria
Повний рік реального акаунта затягується без дублів; kill -9 посеред процесу → після рестарту докачка без втрат/дублів; перші 2 місяці доступні через ~2–3 хв.
