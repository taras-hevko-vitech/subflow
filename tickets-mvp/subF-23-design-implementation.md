# subF-23 · Імплементація дизайну (бренд + дизайн-система + екрани + анімації)
**Фаза:** 3 · **Розмір:** M/L · **Блокери:** фінальний апрув дизайну (робиться зараз, HTML-експорти в `design/`)

## Контекст
Дизайн розробляється окремо і складається в самодостатні HTML-бандли в локальній папці
`design/` (у git не йде, див. `.gitignore`). Уже є «Subflow — Дизайн-система» і
«Subflow — Екрани». Формат перевірений: усередині секції 01 Кольори (в термінах
Flutter `ColorScheme`) · 02 Типографіка (Rubik + Golos Text, вшиті woff2) ·
03 Spacing/форма · 04 Компоненти · 05 Іконографіка · 06 «theme.dart — мапінг»
(готовий скелет під код).

Орієнтири айдентики: primary `#6B5CE7` · secondary `#FF7A59` · tertiary `#FFC957` ·
surface `#FBF7F0` (light) — фінальні значення брати з останньої версії HTML.

## Задачі

### Зроблено (ітерація 1, 2026-07-07 — гілка feat/subF-23-design)
- [x] `theme.dart`: повний `ColorScheme` light **і dark** із секції 06 + `ThemeExtension<SubflowColors>` (success/warning — значення контейнерів вивів Claude, звірити з фінальним HTML)
- [x] Шрифти: Rubik + Golos Text через google_fonts, суми з `tabularFigures`
- [x] Компонентні теми: stadium-кнопки h52, картки 18, поля 14, діалоги/шіти 26, чипси 10, snackbar, app bar; `AppCard` з перовою тінню в light
- [x] Motion-система (`core/motion.dart`): emphasized-криві, count-up (easeOutExpo + haptic + session-flag + reduce-motion), каскад карток (stagger 60ms), press-scale
- [x] Aha-екран: hero за макетом, чипси-фільтри (Усі/Скоро/Нові), нові картки з пігулками (завтра/через N дн./нова/заснула)
- [x] Онбординг: SharedAxis X переходи (пакет animations), прогрес-бар замість крапок
- [x] Progress-екран: сітка 52 тижнів (overshoot + пульс поточного), плавний %, ротація повідомлень з реальними знахідками, «Згорнути — сповістимо»
- [x] Нижня навігація (Головна · Сповіщення · Профіль) + стаб екрана сповіщень
- [x] Логін + «Перевір пошту» (таймер повторної відправки, «Не той email?»)
- [x] Профіль за макетом + видалення type-to-confirm «ВИДАЛИТИ»
- [x] Фідбек з причинами (не підписка / сума-дата / дублікат / інше)
- [x] Share-картка у новому бренді (violet→ink, топ-підписки, «А скільки з'їдають твої?»)

### Лишилось
- [ ] Лого/іконка застосунку (iOS + Android adaptive) — чекає Етап 1 (знак)
- [ ] Кастомний pull-to-refresh зі знаком Subflow (motion-спека §4) — блокується знаком з Етапу 1
- [ ] Детальна картка підписки: графік історії списань за 12 міс, події «ціна зросла», блок «Як скасувати» за макетом
- [ ] Екран «Безпека і приватність» за макетом (токен-статус, шифрування, завантажити дані, політика)
- [ ] Екран сповіщень зі стрічкою — блокується subF-16 (пуші)
- [ ] Банер «Доступ втрачено» в профілі й на головній за макетом
- [ ] Консистентний символ валюти: гривневі показують «₴945», валютні (код 840) — голе число; показувати ₴ завжди (суми і так у гривні) або «$52.84 ≈ ₴5 405»
- [ ] Прогін dark-теми по всіх екранах + Android

## Верифікація
Живий прогін у симуляторі по всіх флоу + скріншоти до/після по кожному екрану.
Ітерація 1 перевірена наживо: light-тема, логін → aha-екран з реальними даними (₴1 849/міс, фільтри, пігулки дат).
